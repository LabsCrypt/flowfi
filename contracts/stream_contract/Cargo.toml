[package]
name = "stream_contract"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
proptest = "1.11.0"
soroban-sdk = { workspace = true, features = ["testutils"] }
