{
  "name": "flowfi",
  "version": "1.0.0",
  "description": "DeFi Payment Streaming on Stellar",
  "type": "module",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "prepare": "node -e \"try { require.resolve('husky'); require('child_process').execSync('npx husky', {stdio: 'inherit'}); } catch(e) {}\"",
    "verify-security": "node scripts/verify-security-setup.js"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  },
  "lint-staged": {
    "frontend/**/*.{ts,tsx,js,jsx}": [
      "npm run lint --workspace=frontend --"
    ]
  },
  "dependencies": {
    "react-hot-toast": "^2.6.0"
  }
}
